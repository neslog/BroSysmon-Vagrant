Vagrant.configure("2") do |config|
  config.vm.box = "neslog/CentOS-BroSysmon"
  config.vm.box_version = "0.1"
  config.vm.network "private_network", :type => 'dhcp', :name => 'intnet', :adapter => 1
end
