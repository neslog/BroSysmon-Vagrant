Vagrant.configure("2") do |config|
  config.vm.box = "neslog/WindowsBroSysmon32"
  config.vm.box_version = "0.1"
  config.vm.network "private_network", :type => 'dhcp', :name => 'vboxnet0', :adapter => 1
end
